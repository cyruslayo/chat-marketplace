import { A2UI_V091_BASIC_CATALOG_ID, type A2UIComponent, type A2UIServerMessage } from "@weaver/core";
import type { HumanHandoffArtifact } from "../../web/src/human-handoff-artifact.js";
import { HUMAN_HANDOFF_REQUEST_EVENT, HUMAN_HANDOFF_STOP_EVENT } from "../../web/src/human-handoff-actions.js";
export function humanHandoffArtifactToA2UI({ artifact, surfaceId }: { readonly artifact: HumanHandoffArtifact; readonly surfaceId: string }): readonly A2UIServerMessage[] { const { facts } = artifact; const components: A2UIComponent[] = [{ id: "handoff-root", component: "Column", children: ["handoff-title", "handoff-state"] }, { id: "handoff-title", component: "Text", text: "Concierge Support", variant: "h2" }, { id: "handoff-state", component: "Text", text: stateText(artifact) }]; for (const action of artifact.actions) { const event = action.type === "stop_current_run" ? HUMAN_HANDOFF_STOP_EVENT : HUMAN_HANDOFF_REQUEST_EVENT; const label = action.type === "stop_current_run" ? "Stop current run" : "Talk to a person"; components.push({ id: `handoff-${action.type}`, component: "Button", child: `handoff-${action.type}-label`, variant: "primary", action: { event: { name: event, context: { artifactId: action.artifactId, threadId: action.threadId, expectedMode: action.expectedMode, projectionVersion: action.projectionVersion } } }, accessibility: { label } }, { id: `handoff-${action.type}-label`, component: "Text", text: label }); } return Object.freeze([{ version: "v0.9.1", createSurface: { surfaceId, catalogId: A2UI_V091_BASIC_CATALOG_ID } }, { version: "v0.9.1", updateComponents: { surfaceId, components } }]); }
function stateText(artifact: HumanHandoffArtifact): string { const { facts } = artifact; if (facts.mode === "handoff-requested") return `Human Support requested${facts.supportLabel ? ` via ${facts.supportLabel}` : ""}.`; if (facts.mode === "human-owned") return "A Human Support responder is handling this matter. Automated changes are paused."; if (facts.mode === "resume-pending") return "Human Support has completed handback; the concierge is refreshing the current state."; return facts.lastUserNotice ? `Concierge resumed. ${facts.lastUserNotice}` : "Your concierge is ready."; }
export { humanHandoffArtifactToA2UI as handoffArtifactToA2UI };
