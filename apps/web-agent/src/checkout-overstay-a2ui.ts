import { A2UI_V091_BASIC_CATALOG_ID, type A2UIComponent, type A2UIServerMessage } from "@weaver/core";
import type { CheckoutArtifact } from "../../web/src/checkout-overstay-artifact.js";
import { CHECKOUT_ACCEPT_LATE_CHECKOUT_EVENT } from "../../web/src/checkout-overstay-actions.js";
export function checkoutArtifactToA2UI({ artifact, surfaceId }: { readonly artifact: CheckoutArtifact; readonly surfaceId: string }): readonly A2UIServerMessage[] { const f = artifact.facts; const components: A2UIComponent[] = [{ id: "checkout-root", component: "Column", children: ["checkout-title", "checkout-effective", "checkout-deadlines", "checkout-status", "checkout-options"] }, { id: "checkout-title", component: "Text", text: "Checkout", variant: "h2" }, { id: "checkout-effective", component: "Text", text: `Effective Checkout: ${f.effectiveCheckoutTime} WAT` }, { id: "checkout-deadlines", component: "Text", text: `Access expiry: ${f.accessExpiryIso}; turnover starts: ${f.turnoverStartIso}; deposit claim deadline: ${f.depositClaimDeadlineIso}` }, { id: "checkout-status", component: "Text", text: f.overstayStatus === "none" ? (f.humanOwned ? "Human Support is handling this checkout." : f.humanSupportRequested ? "Human Support requested." : "No overstay incident is open.") : "An unauthorized-overstay incident is open." }, ...f.lateCheckoutOptions.flatMap((option): A2UIComponent[] => { const action = artifact.actions.find((candidate) => candidate.requestedTime === option.requestedTime); if (!action) return []; const label = `Late Checkout to ${option.requestedTime} WAT — ${option.currency} ${(option.feeKobo / 100).toLocaleString("en-NG", { minimumFractionDigits: 2 })}`; return [{ id: `checkout-option-${option.requestedTime}`, component: "Button", child: `checkout-label-${option.requestedTime}`, variant: "primary", action: { event: { name: CHECKOUT_ACCEPT_LATE_CHECKOUT_EVENT, context: { artifactId: action.artifactId, reservationId: action.reservationId, requestedTime: action.requestedTime, quoteId: action.quoteId, expectedCheckoutTime: action.expectedCheckoutTime, expectedTermVersion: action.expectedTermVersion, projectionVersion: action.projectionVersion } } }, accessibility: { label } }, { id: `checkout-label-${option.requestedTime}`, component: "Text", text: label }]; })]; return Object.freeze([{ version: "v0.9.1", createSurface: { surfaceId, catalogId: A2UI_V091_BASIC_CATALOG_ID } }, { version: "v0.9.1", updateComponents: { surfaceId, components } }]); }
export { checkoutArtifactToA2UI as checkoutOverstayArtifactToA2UI };
