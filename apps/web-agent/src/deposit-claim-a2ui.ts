import { A2UI_V091_BASIC_CATALOG_ID, type A2UIComponent, type A2UIServerMessage } from "@weaver/core";
import type { DepositClaimArtifact } from "../../web/src/deposit-claim-artifact.js";
import { EVENTS } from "../../web/src/deposit-claim-actions.js";
export function depositClaimArtifactToA2UI({ artifact, surfaceId }: { readonly artifact: DepositClaimArtifact; readonly surfaceId: string }): readonly A2UIServerMessage[] { const f = artifact.facts; const components: A2UIComponent[] = [{ id: "deposit-claim-root", component: "Column", children: ["deposit-claim-title", "deposit-claim-summary", "deposit-claim-items", "deposit-claim-actions"] }, { id: "deposit-claim-title", component: "Text", text: "Security Deposit Claim", variant: "h2" }, { id: "deposit-claim-summary", component: "Text", text: `Held deposit: NGN ${(f.depositAmountKobo / 100).toLocaleString("en-NG", { minimumFractionDigits: 2 })}. Validated claim: NGN ${(f.claimedAmountKobo / 100).toLocaleString("en-NG", { minimumFractionDigits: 2 })}. Status: ${f.status}.` }, { id: "deposit-claim-items", component: "Text", text: f.items.map((item) => `${item.description}: NGN ${(item.claimedAmountKobo / 100).toLocaleString("en-NG", { minimumFractionDigits: 2 })}`).join("; ") }, { id: "deposit-claim-actions", component: "Column", children: artifact.actions.map((action) => `deposit-action-${action.type}`) }, ...artifact.actions.flatMap((action): A2UIComponent[] => [{ id: `deposit-action-${action.type}`, component: "Button", child: `deposit-label-${action.type}`, variant: action.type === "dispute" ? "secondary" : "primary", action: { event: { name: EVENTS[action.type], context: { artifactId: action.artifactId, claimId: action.claimId, expectedClaimVersion: action.expectedClaimVersion, projectionVersion: action.projectionVersion } } } }, { id: `deposit-label-${action.type}`, component: "Text", text: action.type === "accept" ? "Accept this claim (reserve remains under review)" : action.type === "dispute" ? "Dispute this claim" : "Apply reviewed human decision" }])]; return Object.freeze([{ version: "v0.9.1", createSurface: { surfaceId, catalogId: A2UI_V091_BASIC_CATALOG_ID } }, { version: "v0.9.1", updateComponents: { surfaceId, components } }]); }
