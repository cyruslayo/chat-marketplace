export const CANCELLATION_CANCEL_EVENT = "shortlet.cancellation.cancel" as const;
export const CANCELLATION_REVIEW_EVENT = "shortlet.cancellation.apply-reviewed" as const;
export const CANCELLATION_NO_SHOW_EVENT = "shortlet.cancellation.confirm-no-show" as const;
export type CancellationActionContext = Readonly<{ artifactId: string; reservationId: string; expectedReservationStatus: "confirmed"; expectedContractVersion: number; policyVersion: string; economicsVersion: string; projectionVersion: string; decisionVersion?: string; contactAttemptVersion?: string }>;
export function cancellationActionContext(action: { readonly artifactId: string; readonly reservationId: string; readonly expectedReservationStatus: "confirmed"; readonly expectedContractVersion: number; readonly policyVersion: string; readonly economicsVersion: string; readonly projectionVersion: string }): CancellationActionContext { return Object.freeze({ artifactId: action.artifactId, reservationId: action.reservationId, expectedReservationStatus: action.expectedReservationStatus, expectedContractVersion: action.expectedContractVersion, policyVersion: action.policyVersion, economicsVersion: action.economicsVersion, projectionVersion: action.projectionVersion }); }
