import { createPlatformCommandEnvelope, type CommandPrincipal } from "../../../packages/platform-core/src/index.js";
import type { BookingStateRepository } from "../../../domains/shortlet/src/booking-state.js";
import type { BookingContract, Reservation } from "../../../domains/shortlet/src/card-payment.js";
import { RevenueReleaseManager, type AuthoritativeRevenueEconomics, type ProductionRevenueReleaseRecord } from "../../../domains/shortlet/src/revenue-release.js";
import type { RevenueAccountingRepository, RevenueAdjustmentRecord } from "../../../domains/shortlet/src/revenue-accounting.js";
import { revenueReleaseArtifact, type RevenueReleaseArtifact } from "./revenue-release-artifact.js";

export interface RevenueReleaseAccessProvider { getAccess(reservationId: string): { readonly version: string; readonly status: "awaiting_access" | "verified_access" | "late_voluntary_arrival" | "failed_access" | "under_human_review"; readonly verifiedAt?: string }; }
export interface RevenueReleaseHoldProvider { getHold(reservationId: string): { readonly holdVersion: string; readonly blocked: boolean; readonly reasonCodes: readonly string[] }; }
export interface OperatorPaymentAccountProvider { getStatus(operatorId: string): { readonly version: string; readonly active: boolean }; }
export interface RevenueEconomicsProvider { getEconomics(input: { reservationId: string; contract: BookingContract }): AuthoritativeRevenueEconomics; }
export interface RevenuePayoutPlanProvider { getPlan(input: { reservationId: string; operatorId: string; contract: BookingContract }): { readonly planVersion: string; readonly plan: "fast_payout" | "full_post_stay" }; }
export interface RevenueReleaseCheckoutTermsProvider { getTerms(reservationId: string): { readonly checkoutIso: string; readonly version: string }; }
export interface RevenueRiskHoldProvider { getHold(input: { reservationId: string; operatorId: string }): { readonly version: string; readonly amountKobo: number }; }
export interface RevenueAdjustmentAuthority { getAdjustment(adjustmentRef: string): RevenueAdjustmentRecord; }
export interface RevenueReleaseApplicationOptions { readonly bookingState: BookingStateRepository; readonly manager: RevenueReleaseManager; readonly accounting: RevenueAccountingRepository; readonly access: RevenueReleaseAccessProvider; readonly complaints: { hasUnresolvedBlockingComplaint(reservationId: string): boolean }; readonly holds: RevenueReleaseHoldProvider; readonly accounts: OperatorPaymentAccountProvider; readonly economics: RevenueEconomicsProvider; readonly payoutPlans: RevenuePayoutPlanProvider; readonly checkout: RevenueReleaseCheckoutTermsProvider; readonly risk: RevenueRiskHoldProvider; readonly adjustments: RevenueAdjustmentAuthority; readonly consequence?: { isAccommodationActive(reservationId: string): boolean }; readonly audit?: { record(entry: Record<string, unknown>): void }; readonly clock: () => Date; }
const trustedRoles = new Set<CommandPrincipal["role"]>(["system", "admin", "authorized_staff"]);
function deny(): never { throw new Error("Access denied or reservation not found"); }
function backend(principal: CommandPrincipal, tenantId: string): void { if (!trustedRoles.has(principal.role) || !principal.id || !tenantId || principal.tenantId !== tenantId) throw new Error("Trusted backend principal with matching tenant is required"); }
function load(state: BookingStateRepository, id: string): { reservation: Reservation; contract: BookingContract } { const reservation = state.findReservationById(id); if (!reservation) deny(); const contract = state.findContractById(reservation.contractId); if (!contract || contract.reservationId !== id || contract.contractId !== reservation.contractId) deny(); return { reservation, contract }; }
function paid(c: BookingContract): void { if (!c.paymentDetails || c.paymentDetails.currency !== "NGN" || !Number.isInteger(c.paymentDetails.amountKobo) || c.paymentDetails.amountKobo <= 0 || !c.paymentDetails.paidAt || (c.paymentDetails.paymentMethod !== "fresh_card" && c.paymentDetails.paymentMethod !== "bank_transfer")) throw new Error("Booking Contract payment is not authoritative and successful"); }
export class RevenueReleaseApplication {
  readonly #o: RevenueReleaseApplicationOptions;
  constructor(options: RevenueReleaseApplicationOptions) { this.#o = options; }
  #view(reservationId: string, principal: CommandPrincipal): ProductionRevenueReleaseRecord | null { const { reservation, contract } = load(this.#o.bookingState, reservationId); if (principal.role === "operator") { if (!principal.id || principal.id !== contract.parties.operator.id || principal.tenantId !== contract.tenantId) deny(); } else if (principal.role === "authorized_staff" || principal.role === "admin") { if (principal.tenantId !== contract.tenantId) deny(); } else deny(); const found = this.#o.accounting.findReleaseByReservationId(reservationId); return found as ProductionRevenueReleaseRecord | null; }
  getArtifact(reservationId: string, principal: CommandPrincipal): RevenueReleaseArtifact { const { reservation, contract } = load(this.#o.bookingState, reservationId); if (principal.role === "operator") { if (!principal.id || principal.id !== contract.parties.operator.id || principal.tenantId !== contract.tenantId) deny(); } else if ((principal.role === "authorized_staff" || principal.role === "admin") && principal.tenantId === contract.tenantId) { /* trusted support view */ } else deny(); const release = this.#o.accounting.findReleaseByReservationId(reservationId) as ProductionRevenueReleaseRecord | null; const access = this.#o.access.getAccess(reservationId); const terms = this.#o.checkout.getTerms(reservationId); const plan = this.#o.payoutPlans.getPlan({ reservationId, operatorId: contract.parties.operator.id, contract }); const account = this.#o.accounts.getStatus(contract.parties.operator.id); const economics = this.#o.economics.getEconomics({ reservationId, contract }); const hold = this.#o.holds.getHold(reservationId); const risk = this.#o.risk.getHold({ reservationId, operatorId: contract.parties.operator.id }); return revenueReleaseArtifact({ reservation, contract, release, access, plan, account, economics, hold, risk, checkoutVersion: terms.version, adjustments: release ? this.#o.accounting.findLedgerEntriesForRelease(release.releaseId).map((j) => j.journalId) : [] }); }
  releaseRevenue(reservationId: string, principal: CommandPrincipal): ProductionRevenueReleaseRecord { const { reservation, contract } = load(this.#o.bookingState, reservationId); backend(principal, contract.tenantId ?? ""); if (reservation.status !== "confirmed") throw new Error("Revenue release requires a confirmed Reservation"); if (this.#o.consequence && !this.#o.consequence.isAccommodationActive(reservationId)) throw new Error("Original accommodation is no longer active"); paid(contract); const access = this.#o.access.getAccess(reservationId); if ((access.status !== "verified_access" && access.status !== "late_voluntary_arrival") || !access.verifiedAt) throw new Error("Revenue release requires authoritative Verified Access"); if (this.#o.complaints.hasUnresolvedBlockingComplaint(reservationId)) throw new Error("Revenue release blocked: unresolved Blocking Fulfilment Complaint exists"); const hold = this.#o.holds.getHold(reservationId); if (hold.blocked) throw new Error(`Revenue release blocked: ${hold.reasonCodes.join(",")}`); const account = this.#o.accounts.getStatus(contract.parties.operator.id); if (!account.active) throw new Error("Revenue release requires an active Operator payment account"); const economics = this.#o.economics.getEconomics({ reservationId, contract }); const payout = this.#o.payoutPlans.getPlan({ reservationId, operatorId: contract.parties.operator.id, contract }); const checkout = this.#o.checkout.getTerms(reservationId); const risk = this.#o.risk.getHold({ reservationId, operatorId: contract.parties.operator.id }); if (!Number.isInteger(risk.amountKobo) || risk.amountKobo < 0) throw new Error("Invalid authoritative risk hold"); const envelope = createPlatformCommandEnvelope({ commandName: "revenue_release.release", principal, payload: { reservationId }, idempotencyKey: `revenue-release:${reservationId}` }); const result = this.#o.manager.commitProductionRelease({ reservationId, contractId: contract.contractId, contractVersion: contract.contractVersion, unitId: contract.unitId, tenantId: contract.tenantId ?? "", operatorId: contract.parties.operator.id, accessVersion: access.version, accessStatus: access.status, verifiedAccessAt: access.verifiedAt, economics, payoutPlan: payout.plan, payoutPlanVersion: payout.planVersion, effectiveCheckoutAt: checkout.checkoutIso, effectiveCheckoutVersion: checkout.version, riskHoldVersion: risk.version, riskHoldKobo: risk.amountKobo, now: this.#o.clock() }, this.#o.accounting); this.#o.audit?.record({ type: "revenue_release.committed", releaseId: result.releaseId, reservationId, contractVersion: contract.contractVersion, accessVersion: access.version, economicsVersion: economics.economicsVersion, commissionPolicyVersion: economics.commissionPolicyVersion, commissionRate: economics.capturedCommissionRate, commissionBaseKobo: economics.commissionableOperatorRevenueKobo, commissionKobo: result.commissionKobo, operatorNetKobo: result.operatorNetKobo, payoutPlan: payout.plan, payoutPlanVersion: payout.planVersion, payableNowKobo: result.payableNowKobo, routineReserveTrancheKobo: result.routineReserveTrancheKobo, deferredPostStayKobo: result.deferredPostStayKobo, riskHoldVersion: risk.version, riskHoldKobo: risk.amountKobo, ledgerJournalId: result.ledgerJournalId, commandId: envelope.commandId, serverTimestamp: result.releasedAt }); return result; }
  postAdjustment(adjustmentRef: string, principal: CommandPrincipal): RevenueAdjustmentRecord { const adjustment = this.#o.adjustments.getAdjustment(adjustmentRef); const { contract } = load(this.#o.bookingState, adjustment.reservationId); backend(principal, contract.tenantId ?? ""); const release = this.#o.accounting.findReleaseByReservationId(adjustment.reservationId) as ProductionRevenueReleaseRecord | null; if (!release || release.releaseId !== adjustment.releaseId) throw new Error("Adjustment is not correlated to an existing Revenue Release"); const envelope = createPlatformCommandEnvelope({ commandName: "revenue_release.post-adjustment", principal, payload: { adjustmentRef } }); const posted = this.#o.accounting.postAdjustment({ adjustment: { ...adjustment, journal: { ...adjustment.journal, correlationId: release.releaseId } } }); this.#o.audit?.record({ type: "revenue_release.adjustment_posted", adjustmentId: posted.adjustmentId, adjustmentVersion: posted.adjustmentVersion, reservationId: posted.reservationId, releaseId: posted.releaseId, ledgerJournalId: posted.journal.journalId, commandId: envelope.commandId, serverTimestamp: this.#o.clock().toISOString() }); return posted; }
}
